clc;clear all;
[u,t] = gensig("square",2e-03,4e-03);
u=24*u-12;

 
x1=0
x2=0

R=4.7e03; L=10e-06; C=100e-09;
A = [-R/L -1/L; 1/C 0]
B = [1/L; 0]
C= [R 0]
D= [0]
x0 = [x1 x2]

% Creamos modelo de espacio de estado
sys = ss(A,B,C,D)


 [i,vr]=lsim(A,B,C,D,u,t)
 
 subplot(3,1,1);plot(t,i);grid on; title('Corriente'); hold on%plot(t,y);
 subplot(3,1,2);plot(t,v_c);grid on; title(''); hold on
 subplot(3,1,3);plot(t,u);grid on; title('Tensión de entrada'); hold on